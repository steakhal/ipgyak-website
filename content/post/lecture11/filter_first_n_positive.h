#ifndef FILTER_FIRST_N_POSITIVE_H
#define FILTER_FIRST_N_POSITIVE_H

#include <stddef.h>

// MUST FREE the returned pointer
int *filter_first_n_positive(int *nums, size_t nums_size, size_t n);

#endif // FILTER_FIRST_N_POSITIVE_H

